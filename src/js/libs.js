/**
 * si un componente que es utilizado por otro se define despues
 * NO se podra acceder a sus metodos y propiedades, por lo que los componentes que se utilizan 
 * por otros deben ser los primereos en definirse
 */
//componentes pricipales para el control del escritorio
import './bandeja/libs.js';
import './ventanas/libs.js';

import './ui/ui.js';

import './apps/apps.js';


